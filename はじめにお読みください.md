# 議事録レコーダー — 使い方

会議を録音して、**文字起こし → 誰が話したかの判別 → 議事録の自動作成**までを、
**あなたの Mac の中だけ**で行うアプリです。音声が外部に送られることはありません。

---

## 1. 最初の準備（1回だけ・10〜20分）

1. このフォルダを **アプリケーションフォルダや書類フォルダなど、消さない場所** に置いてください
   （※ ダウンロードフォルダのままだと、間違って消しやすいのでおすすめしません）
2. フォルダの中の **`setup.sh`** を **右クリック →「ターミナルで開く」** を選びます
   - 「ターミナルで開く」が無い場合は、`setup.sh` をダブルクリックしてください
3. 黒い画面が開き、自動で準備が進みます。**「完了しました」と出るまで待ちます**（音声モデルのDLで時間がかかります）
4. 終わると **「議事録レコーダー」アプリ** がフォルダの中にできます

準備中に Python が無いと言われた場合は、画面に出るリンクから Python をインストールして、もう一度 `setup.sh` を実行してください。

## 2. 使い方

1. **「議事録レコーダー」をダブルクリック** → ブラウザに画面が開きます
   - よく使うなら、アイコンを Dock にドラッグしておくと 1 クリックで起動できます
2. 会議名を入れて（空欄でもOK）、**赤いボタンをクリック**すると録音開始
   - 初回だけブラウザが「マイクを使ってよいか」と聞くので **許可** してください
3. 録音中は画面に **「● 録音中」** と経過時間・音量メーターが出ます
   - 右側に文字起こしが約10秒ごとに追記され、左側の議事録が約75秒ごとに自動更新されます
4. 終わったら **「終了して議事録にする」** をクリック
   - 話者の判別 → 議事録の作成が走ります（30分の会議で4〜5分ほど）

## 3. できあがるもの

終了すると、フォルダ内の `data/meetings/日付-時刻/` に4つ保存されます。

| ファイル | 用途 |
|---|---|
| `minutes.md` | 議事録。**Notion にそのまま貼れます**（完了画面の「Markdownをコピー」ボタンが便利） |
| `議事録_会議名_日付.html` | **人に送る用**。ダブルクリックで開ける1ファイル |
| `transcript.md` | 発言の全文（時刻・話者つき） |
| `audio.wav` | 元の音声 |

議事録には **要約・決定事項・ネクストアクション（担当と期限の表）・議題ごとの詳細** が入ります。

### 人に送るとき（Slack・メール）

終了画面の **「HTML をコピー（Slack に貼付）」** を押すと、HTMLファイルそのものがコピーされます。
そのまま Slack のメッセージ欄で **⌘V** を押せば添付できます（メールの本文でも同じ）。
中身を先に確認したいときは「HTML を開いて確認」、フォルダを開きたいときは「Finder で表示」です。

送られた相手は、**ダブルクリックするだけでブラウザで読めます**（アプリのインストールは不要）。

## 4. 用語辞書（誤変換を減らす）

社名・人名・専門用語は、そのままだと違う漢字に変換されがちです（例:「茅ヶ崎弁当」→「千ヶ崎弁当」）。
アプリの最初の画面で **「用語辞書を編集」** をクリックすると、一覧で追加・修正できます。

- **読み**はひらがな（例: `ちがさきべんとう`）、**正式表記**は書き起こしたい形（例: `茅ヶ崎弁当`）
- 「＋ 用語を追加」で行を足し、**「保存する」**で反映されます
- **録音中に保存しても、次の文字起こしから効きます**
- よく間違われる自社名・取引先名・社内メンバーの名前を最初に入れておくのがおすすめです

## 5. 知っておいてほしいこと

- **インターネットが無くても、録音と文字起こしは動きます**（議事録の文章化だけはネットが必要）
- **議事録の自動作成には Claude Code（Max または Pro 契約）が必要です**。無い場合は文字起こしまで使えます
- **イヤホンをしてオンライン会議をすると、相手の声が録れません**（マイクが拾う音だけを録音するため）。
  スピーカーで会議するか、対面の会議でお使いください
- 話者の判別は声質で行うため、**声が似ている人は混ざることがあります**。目安としてお使いください
- **録音するときは、参加者に一言伝えてください**

## 6. アップデート

新しい版が出ると、アプリの最初の画面に **「新しい版 V◯◯ があります」** と出ます。
**「いま更新する」を押すだけ**で終わります（1分ほど）。

**録音データ・用語辞書・音声モデルはそのまま残ります。** 入れ替わるのはプログラムだけなので、
630MB のダウンロードをやり直す必要も、zip を配り直してもらう必要もありません。

## 7. 困ったとき

| 症状 | 対処 |
|---|---|
| ダブルクリックしても何も起きない | `setup.sh` をもう一度実行してください |
| 「議事録AI: claude なし」と出る | ターミナルで `claude` と打ってログインしてください |
| 録音ボタンを押しても始まらない | ブラウザの画面を再読み込み（⌘R）してください |
| 文字起こしの精度を上げたい | 「用語辞書を編集」から固有名詞（社名・人名）を追加してください |

それでも解決しない場合は、フォルダ内の `data/server.log` を管理者に送ってください。
